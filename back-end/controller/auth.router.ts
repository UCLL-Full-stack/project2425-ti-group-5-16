// src/router/auth.router.ts
import express, { Request, Response, NextFunction } from 'express';

// IK KAN DEZE FILE NIET VERWIJDEREN, WANT DAN ZIJN ER ERRORS IN DE NODE_MODULES

const authRouter = express.Router();

export { authRouter };
